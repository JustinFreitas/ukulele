{
  "name": "ukulele",
  "version": "2.26.0",
  "private": true,
  "description": "Discord music bot (Java/Kotlin, Spring Boot). package.json exists only so PM2 shows a version in `pm2 list`. Keep \"version\" in step with `version` in build.gradle.kts when cutting a release."
}
